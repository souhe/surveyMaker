/** @jsx React.DOM */
var React = require('react');

var matrixQuestionInEditMode = React.createClass({
    render: function(){
        return(
            <div>editing Matrix</div>
        );
    }
});

module.exports = matrixQuestionInEditMode;